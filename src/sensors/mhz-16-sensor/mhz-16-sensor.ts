import Sensor from '../sensor';

export class Mhz16Sensor extends Sensor{
  // constructor(options) {
  //   super(options);
  // }

  initMhz16(options) {
    console.log('My name is', options.name);
  }
}
